{
  "name": "simplified-angular-cli",
  "version": "0.0.5",
  "description": "simplified version of @angular/cli for demo",
  "main": "index.js",
  "bin": {
    "sng": "./sng"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "version": "conventional-changelog -p angular -i STEPS.md -s && git add STEPS.md"
  },
  "author": "Noël Macé <contact@noelmace.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.18",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "@angular-devkit/architect": "^0.12.1",
    "@angular-devkit/core": "^7.2.1"
  }
}
